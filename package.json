{
  "name": "telkit-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.3.14",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "bun run --filter telkit dev",
    "dev:local-mcp": "bun run --filter telkit-mcp dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "oxlint --deny-warnings packages",
    "lint:fix": "oxlint --fix --deny-warnings packages",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "oxlint": "1.79.0",
    "prettier": "3.9.6",
    "typescript": "7.0.2"
  }
}
